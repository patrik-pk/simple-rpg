import {

} from '../actions/types'

const initialState = {
    cs: 'cs'
}

export default (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}